package org.modelix.model.mpsplugin;

/*Generated by MPS */

import java.util.List;
import org.modelix.model.api.INode;
import jetbrains.mps.internal.collections.runtime.ListSequence;
import java.util.LinkedList;
import java.util.Iterator;

public class IterableOfINodeUtils {
  public IterableOfINodeUtils() {
  }
  public static List<INode> toList(final Iterable<INode> _this) {
    List<INode> res = ListSequence.fromList(new LinkedList<INode>());
    for (Iterator<INode> it = _this.iterator(); it.hasNext();) {
      ListSequence.fromList(res).addElement(it.next());
    }
    return res;
  }
  public static <T extends INode> List<T> toCastedList(final Iterable<INode> _this) {
    List<T> res = ListSequence.fromList(new LinkedList<T>());
    for (Iterator<INode> it = _this.iterator(); it.hasNext();) {
      ListSequence.fromList(res).addElement(((T) it.next()));
    }
    return res;
  }
}
